import { ScrollRevealDirective } from './scroll';

describe('Scroll', () => {
  it('should create an instance', () => {
    const directive = new Scroll();
    expect(directive).toBeTruthy();
  });
});
